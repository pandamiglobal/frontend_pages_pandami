/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      'cms.pandami.com.br',
      'pandami.com.br',
      'images.pexels.com'
    ],
  }
};

module.exports = nextConfig; 