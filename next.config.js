/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      'cms.pppi.com.br',
      'pppi.com.br',
      'images.pexels.com'
    ],
  }
};

module.exports = nextConfig; 