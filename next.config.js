/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      'cms.pppi.com.br',
      'pppi.com.br'
    ],
  },
};

module.exports = nextConfig; 